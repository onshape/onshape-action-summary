name: 'onshape-action-summary'

runs:
  using: "composite"
  steps:
    - name: get-summary
      shell: bash
      run: |
        bash ${GITHUB_WORKSPACE}/action-summary.bash
      env:
        INPUT_STRING: "Hello there"
